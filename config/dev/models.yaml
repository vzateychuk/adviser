version: "1.0"

models:
  planner:
    # Быстрая логическая модель для классификации
    primary: "qwen3.5:cloud" 
    fallback: "glm-5:cloud"

  ocr_executor:
    # Специализированная модель для работы с текстом и кодом
    primary: "kimi-k2.5:cloud" 
    fallback: "qwen3.5:cloud"

  critic:
    # Компактная, но строгая модель для проверки
    primary: "llama3.3:cloud"
    fallback: "phi-4:cloud"

  default:
    primary: "qwen3.5:cloud"
    fallback: "llama3.2:1b" # Локальный fallback на случай сбоя сети