version: "1.0"

llm:
  provider: "openai" # Или "openai", если используется совместимый API Ollama
  base_url: "http://localhost:11434/v1" # Стандартный порт Ollama с поддержкой v1 API
  timeout: 300

db:
  path: ".data/db/advisor.sqlite"

prompts_dir: "prompts"

orchestrator:
  max_retries: 2