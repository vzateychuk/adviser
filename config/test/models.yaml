version: "1.0"

models:
  planner:
    primary: "mock"
    fallback: "mock"
  generic_executor:
    primary: "mock"
    fallback: "mock"
  code_executor:
    primary: "mock"
    fallback: "mock"
  reviewer:
    primary: "mock"
    fallback: "mock"